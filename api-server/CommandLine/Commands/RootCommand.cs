namespace CS.CommandLine.Commands;

public class RootCommand : System.CommandLine.RootCommand
{
    public RootCommand() : base("CommandLine utility for the Core API")
    {
        //AddCommand(new Command());
    }
}

